services:
  mysql:
    image: mysql:8.4
    container_name: sdj-demo-mysql
    restart: unless-stopped
    ports:
      - "3006:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
